describe('Dummy', () => {
  it('should pass', () => {
    expect(1).toBe(1);
  });
});
  // @IsOptional()
  // @Type(() => Number)
  // @IsInt()
  // @Min(1)
  // limit?: number = 20;

// const mockRedisClient = {
//   connect: jest.fn(),
//   get: jest.fn(),
//   set: jest.fn(),
//   del: jest.fn(),
// };
